"""
Dialog helpers — Phase 1.

Planned API
-----------
def confirm_dialog(parent, title, message) -> bool
def input_dialog(parent, title, label, default="") -> str | None
def error_dialog(parent, title, message) -> None
"""
